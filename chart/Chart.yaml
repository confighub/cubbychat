apiVersion: v2
name: cubbychat
description: >-
  CubbyChat — a small AI chat app: frontend, backend, and a bundled
  PostgreSQL StatefulSet, with an optional Ollama deployment (ai.enabled).
  Authored as a ConfigHub delivery chart: hook-free, renders fully
  client-side, one template file per unit.
type: application
version: 0.1.0
appVersion: "1.1.7"
